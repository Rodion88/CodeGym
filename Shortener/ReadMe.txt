Shortener

Shortener. It's kind of like a shortener.
links Google URL Shortener (https://goo.gl), but extended its functionality and
made it console. It shortens not only links, but also any strings.
Shortener is a class that can return some string for any string.
a unique identifier and vice versa, by a previously received identifier
return a string.
